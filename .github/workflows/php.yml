
name: App Workflow

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    name: Tests
    runs-on: ubuntu-latest
    steps:

      - name: Installing Composer
        run: ./.github/scripts/run-composer.sh
